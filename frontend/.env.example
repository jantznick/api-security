# Leave empty to use Vite proxy (/api → localhost:3001) for local development.
VITE_API_URL=""

# Production (Render static site build): public core Railway URL (no trailing slash).
# Example: VITE_API_URL=https://core-production-xxxx.up.railway.app
# The client appends /api automatically.
