{
  "name": "api-security-ingest",
  "version": "0.1.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "npm run prisma:generate && node --watch server.js",
    "start": "node ../backend/scripts/safe-migrate.js && node server.js",
    "postinstall": "prisma generate --schema=../backend/prisma/schema.prisma",
    "prisma:generate": "prisma generate --schema=../backend/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
