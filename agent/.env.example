PORT=8080
# Local agent: localhost. Docker Compose overrides this to host.docker.internal.
INGEST_URL=http://localhost:3002
INGEST_API_KEY=dev-ingest-key-replace-me
AGENT_DEBUG_BUFFER=false
AGENT_FLUSH_MS=2000
